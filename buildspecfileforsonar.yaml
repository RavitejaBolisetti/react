version: 0.2
phases:
    pre_build:
        commands:
           # - n 18
           # - node -v
           # - npm -v
            - npm install -g sonarqube-scanner
            - npm i          
    build:
        commands:
            # - codebuild-breakpoint
            - npm run coverage || true
            
            - sonar-scanner -Dsonar.sources=src -Dsonar.projectKey=dms-ui-application -Dsonar.host.url=http://10.168.12.90:9000/ -Dsonar.login=squ_0fd490ebdba24d2442086bb26b6a424db7c8bd24
            - npm run build
    post_build:
        commands:
            - echo Build completed on `date`
artifacts:
    files:
        - '**/*'
    base-directory: 'build'
cache:
    paths:
        - 'node_modules/**/*'